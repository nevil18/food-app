server {
    listen 80;
    server_name _;

    location /admin {
        alias /usr/share/nginx/html;
        try_files $uri $uri/ /usr/share/nginx/html/index.html;
    }

    location /admin/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /usr/share/nginx/html/index.html;
    }
}
